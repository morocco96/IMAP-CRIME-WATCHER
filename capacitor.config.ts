import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'imap.crime.watcher',
  appName: 'IMAP-Crime-Watcher',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
